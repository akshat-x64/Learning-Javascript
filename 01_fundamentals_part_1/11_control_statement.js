const age = 22;
const leagleAgeForDriving = 18;

//let decision = age >= leagleAgeForDriving;

// if (decision) {
//     console.log(`Akshat can drive and its age is ${age}`)
// }
// else {
//     console.log(`Akshat is too young to drive at ${age}`);
// }

if (age >= leagleAgeForDriving) {
    console.log(`Akshat can drive and its age is ${age}`);
}
else {
    console.log(`Akshat is too young to drive at ${age}`);
}