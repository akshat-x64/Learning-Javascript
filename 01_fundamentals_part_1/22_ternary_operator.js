// in ternary operator writing esle block is compulsory
let age = 21;
age >= 18 ? console.log("Valid for driving") : console.log("not valid for driving");

let decision = age >= 18 ? "yes" : "no";
console.log(`Checking if valid for driving ${age >= 18 ? "yes" : "no"}`)

let personAge = 21;

personAge >= 21 ? console.log(`You can drive`) : console.log(`You cannot drive`)

let $age = 21 >= 20 ? "yes" : "no"

if (age) {
    console.log("You can drive");
}

console.log(`You can drive if your are above 21 ${32 >= 20 ? "Yes" : "no"}`);